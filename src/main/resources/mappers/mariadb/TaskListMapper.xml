<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.todoapp.mappers.TaskListMapper">

    <select id="findNameByIdAndUserId" resultType="string">
        select name from task_list
        where task_list_id = #{taskListId}
        and user_id = #{userId}
    </select>

    <select id="selectNameByUserId" resultType="TaskListName">
        SELECT task_list_id, name, is_default
        FROM task_list
        WHERE user_id = #{userId}
        AND is_deleted = 0
    </select>

    <insert id="insert">
        INSERT INTO task_list (task_list_id, user_id, name, is_default, is_deleted, created_at, updated_at)
        VALUES (#{taskListId}, #{userId}, #{name}, #{isDefault}, #{isDeleted}, now(), now())
    </insert>
</mapper>